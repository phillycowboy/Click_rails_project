<%= render partial: "/errors", locals: {instance: @comment}%>
<%= form_for comment, url: url do |f|%>
    <%= f.label :content %>
    <%= f.text_field :content %>

    <%= f.submit "Add Comment" %>

<% end %>